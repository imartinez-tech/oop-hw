const Engineer = require("../engineer"); 
 
 describe("Engineer", () => {
    it("write a profile for the engineer", () => {
        const pro = new Engineer(); 

        expect("value" in obj).toEqual(true);
    });
 })